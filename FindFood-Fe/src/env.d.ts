// Type declaration for `@env` module used by react-native-dotenv
// Keep this minimal to avoid needing to list every env variable here.
// You can replace `declare module '@env';` with explicit exports for stronger typing.

declare module '@env';
